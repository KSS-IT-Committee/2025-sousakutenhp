---
import SubPageLayout from "./SubPageLayout.astro";
import "../style/exhibits.css";

const { frontmatter } = Astro.props;
---

<SubPageLayout PageTitle={frontmatter.title}>
    <div class="exhibits-detail-container fade-in">
        <!-- Back Navigation -->
        <div class="back-navigation">
            <a href="/page/exhibits" class="back-link">
                <span class="back-icon">←</span>
                <span class="back-text">団体一覧へ戻る</span>
            </a>
        </div>

        <!-- Main Content Card -->
        <div class="exhibits-detail-card">
            <!-- Header Section -->
            <div class="exhibits-detail-header">
                <div class="exhibits-detail-title-section">
                </div>
                <div class="exhibits-detail-meta">
                    <div class="exhibits-detail-room">
                        <span class="room-icon">📍</span>
                        <span class="room-text">{frontmatter.room}</span>
                    </div>
                </div>
            </div>

            <!-- Content Section -->
            <div class="exhibits-detail-body">
                <div class="exhibits-detail-content">
                    <slot />
                </div>
            </div>

            <!-- Footer Section -->
            <div class="exhibits-detail-footer">
                <div class="exhibits-detail-divider"></div>
                <div class="exhibits-detail-actions">
                    <a href="/page/exhibits" class="action-link">
                        <span class="action-icon">🏛️</span>
                        <span class="action-text">他の展示を見る</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</SubPageLayout>
