---
import SubPageLayout from "../../layouts/SubPageLayout.astro";
import ExhibitsList from "../../components/exhibits_list.astro";
import "../../style/exhibits.css";

const risshiExhibits = await Astro.glob("./exhibits/risshi/*.md");
const kaitakuExhibits = await Astro.glob("./exhibits/kaitaku/*.md");
const sousakuExhibits = await Astro.glob("./exhibits/sousaku/*.md");
const clubExhibits = await Astro.glob("./exhibits/club/*.md");
const othersExhibits = await Astro.glob("./exhibits/others/*.md");

const PageTitle = "出展団体紹介";
---

<SubPageLayout PageTitle={PageTitle}>
    <div class="exhibits-container fade-in">
        <!-- Page Introduction -->
        <!-- <div class="exhibits-intro">
            <p class="intro-text">
                創作展では、各学年・部活動による個性豊かな展示・公演が行われます。
                ぜひお気に入りの企画を見つけてください。
            </p>
        </div> -->

        <!-- 立志部門 -->
        <section class="exhibits-section risshi-section" id="risshi">
            <div class="section-header">
                <div class="section-icon">🎨</div>
                <div class="section-content">
                    <h2 class="section-title">立志部門</h2>
                    <p class="section-description">1・2年生による展示企画</p>
                    <div class="section-badge">展示</div>
                </div>
            </div>
            <div class="exhibits-grid">
                {
                    risshiExhibits.map((exhibit) => (
                        <ExhibitsList
                            title={exhibit.frontmatter.title}
                            name={exhibit.frontmatter.name}
                            url={exhibit.url}
                            backgroundColor={
                                exhibit.frontmatter.backgroundColor
                                    ? exhibit.frontmatter.backgroundColor
                                    : "white"
                            }
                        />
                    ))
                }
            </div>
        </section>

        <!-- 開拓部門 -->
        <section class="exhibits-section kaitaku-section" id="kaitaku">
            <div class="section-header">
                <div class="section-icon">🎭</div>
                <div class="section-content">
                    <h2 class="section-title">開拓部門</h2>
                    <p class="section-description">3・4年生による演劇公演</p>
                    <div class="section-badge">演劇</div>
                </div>
            </div>
            <div class="exhibits-grid">
                {
                    kaitakuExhibits.map((exhibit) => (
                        <ExhibitsList
                            title={exhibit.frontmatter.title}
                            name={exhibit.frontmatter.name}
                            url={exhibit.url}
                            backgroundColor={
                                exhibit.frontmatter.backgroundColor
                                    ? exhibit.frontmatter.backgroundColor
                                    : "white"
                            }
                        />
                    ))
                }
            </div>
        </section>

        <!-- 創作部門 -->
        <section class="exhibits-section sousaku-section" id="sousaku">
            <div class="section-header">
                <div class="section-icon">🎪</div>
                <div class="section-content">
                    <h2 class="section-title">創作部門</h2>
                    <p class="section-description">5・6年生による演劇公演</p>
                    <p class="section-description-caution">
                        創作部門の観覧は事前抽選制です。詳しくは<a
                            href="/page/lottery/">こちら</a
                        >をご覧ください
                    </p>

                    <div class="section-badge">演劇</div>
                </div>
            </div>
            <div class="exhibits-grid">
                {
                    sousakuExhibits.map((exhibit) => (
                        <ExhibitsList
                            title={exhibit.frontmatter.title}
                            name={exhibit.frontmatter.name}
                            url={exhibit.url}
                            backgroundColor={
                                exhibit.frontmatter.backgroundColor
                                    ? exhibit.frontmatter.backgroundColor
                                    : "white"
                            }
                        />
                    ))
                }
            </div>
        </section>

        <!-- 部活動団体 -->
        <section class="exhibits-section club-section" id="club">
            <div class="section-header">
                <div class="section-icon">🏛️</div>
                <div class="section-content">
                    <h2 class="section-title">部活動団体</h2>
                    <p class="section-description">部活動による公演・展示</p>
                    <div class="section-badge">部活</div>
                </div>
            </div>
            <div class="exhibits-grid">
                {
                    clubExhibits.map((exhibit) => (
                        <ExhibitsList
                            title={exhibit.frontmatter.title}
                            name={exhibit.frontmatter.name}
                            url={exhibit.url}
                            backgroundColor={
                                exhibit.frontmatter.backgroundColor
                                    ? exhibit.frontmatter.backgroundColor
                                    : "white"
                            }
                        />
                    ))
                }
            </div>
        </section>

        <!-- その他 -->
        <section class="exhibits-section others-section" id="others">
            <div class="section-header">
                <div class="section-icon">✨</div>
                <div class="section-content">
                    <h2 class="section-title">その他</h2>
                    <p class="section-description">その他の団体による企画</p>
                    <div class="section-badge">その他</div>
                </div>
            </div>
            <div class="exhibits-grid">
                {
                    othersExhibits.map((exhibit) => (
                        <ExhibitsList
                            title={exhibit.frontmatter.title}
                            name={exhibit.frontmatter.name}
                            room={exhibit.frontmatter.room}
                            url={exhibit.url}
                            backgroundColor={
                                exhibit.frontmatter.backgroundColor
                                    ? exhibit.frontmatter.backgroundColor
                                    : "white"
                            }
                        />
                    ))
                }
            </div>
        </section>

        <!-- Footer Note -->
        <div class="exhibits-footer">
            <div class="footer-note">
                <p>※ 各展示・公演の詳細は、各団体のページをご覧ください。</p>
                <p>※ 公演時間や展示内容は変更される場合があります。</p>
            </div>
        </div>
    </div>
</SubPageLayout>
